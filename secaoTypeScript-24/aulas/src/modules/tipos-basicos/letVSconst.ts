/* 
    let VS const
    let phrase1 = 'Que a força esteja com você';
    Perceba que a variavl let e do tipo string, ou seja, ela pode receber qualquer valor do tipo string.

    const phrase2 = 'Como deve ser';
    Perceba que a variavel const e do tipo 'como deve ser', semelhante ao literal type.

    Podemos tambem transformar uma variavel let em uma const, utilizando o as const. Ex:
    let phrase3 = 'Como tenho dito' as const
*/

let phrase1 = "Que a força esteja com você";
const phrase2 = "Como deve ser";

let phrase3 = "Como tenho dito" as const;
// const phrase2: 'Como deve ser' = 'Como deve ser';

export default () => {};
